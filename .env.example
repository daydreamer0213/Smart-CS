# SmartCS 配置示例
# 复制为本地 .env 后填写；不要提交真实密钥。

# 数据库
DATABASE_URL=sqlite:///./smartcs.db

# ChromaDB
CHROMA_PERSIST_DIR=./chroma_data

# 兼容 OpenAI 接口的对话模型；以下厂商值只是可替换示例。
LLM_API_KEY=sk-your-key-here
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat
EMBEDDING_PROVIDER=openai
# 无外部 Embedding 额度的离线演示可使用：
# EMBEDDING_PROVIDER=hash
EMBEDDING_API_KEY=sk-your-aliyun-key
EMBEDDING_MODEL=text-embedding-v3
EMBEDDING_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# 缓存
L1_CACHE_TTL=300
L2_CACHE_THRESHOLD=0.85

# 意图识别
INTENT_CONFIDENCE_THRESHOLD=0.6

# 会话
MAX_CONTEXT_TOKENS=2000
MAX_CONVERSATION_TURNS=10

# 限流
RATE_LIMIT_PER_MINUTE=30

# 日志
LOG_LEVEL=INFO
LOG_DIR=logs

# Agent
AGENT_RECURSION_LIMIT=10
AGENT_TIMEOUT_SECONDS=60
# 仅控制模型调用的内部流式参数，不代表对外提供 SSE/WebSocket 接口。
AGENT_STREAM_ENABLED=True

# 身份认证
JWT_SECRET=change-me-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# 可选 Docling/Tesseract 解析器：大型下载、模型和 OCR 数据保留在 D 盘。
PIP_CACHE_DIR=D:/DevData/smartcs/pip
PARSER_DATA_ROOT=D:/DevData/smartcs
DOCUMENT_STORAGE_DIR=D:/DevData/smartcs/documents
PARSER_TEMP_DIR=D:/DevData/smartcs/tmp
DOCLING_ARTIFACTS_PATH=D:/DevData/smartcs/docling/artifacts
HF_HOME=D:/DevData/smartcs/huggingface
TORCH_HOME=D:/DevData/smartcs/torch
TESSERACT_CMD=D:/DevData/smartcs/tesseract-env/Library/bin/tesseract.exe
# Tesseract 语言数据目录必须以斜杠结尾。
TESSDATA_PREFIX=D:/DevData/smartcs/tesseract-env/share/tessdata/
DOCLING_DEVICE=cpu
DOCLING_NUM_THREADS=4
OMP_NUM_THREADS=4
